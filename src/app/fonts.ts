import { Inter, Roboto_Mono } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], variable: '--font-inter'});

export const roboto = Roboto_Mono({ subsets: ["latin"], variable: '--font-roboto', display: 'swap',});