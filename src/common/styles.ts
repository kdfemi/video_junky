const styles = {
    shimAnimation:
  'before:absolute before:inset-0 before:-translate-x-full before:animate-[shim_2s_infinite] before:bg-gradient-to-r before:from-transparent before:via-white/60 before:to-transparent overflow-hidden relative bg-gray-700'
  
};

export default styles;